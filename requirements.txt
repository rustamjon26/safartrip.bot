# Core dependencies
aiogram==3.4.1
python-dotenv==1.0.1

# PostgreSQL async driver (required for booking system)
asyncpg>=0.29.0

# Optional: Redis for FSM persistence (recommended for production)
# Uncomment if using REDIS_URL:
# redis>=5.0.0
